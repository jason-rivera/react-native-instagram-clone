import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  userIcon: {
    width: 80,
    height: 80,
    margin: 5,
    borderRadius: 100,
  },
  userName: {
    textAlign: 'center',
    height: 40,
  },
});

export default styles;
