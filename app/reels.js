import { View, Text, SafeAreaView } from 'react-native';

const Reels = () => {
  return (
    // <SafeAreaView>
    <View>
      <Text>Reels</Text>
    </View>
    // </SafeAreaView>
  );
};

export default Reels;
